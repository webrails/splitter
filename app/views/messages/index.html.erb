<h1>Messages</h1>

<%= link_to "New message", new_message_path %>

<% @messages.each do |message| %>
  <div class="message">
    <p><%= message.text %></p>
    <p><%= l message.created_at %></p>
  </div>
<% end %>
